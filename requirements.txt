# Pinned to the versions the app was built and tested against, so the hosted
# deployment matches local. Bump deliberately, not automatically.
streamlit==1.46.1
streamlit-authenticator==0.4.2
pandas==2.3.1
numpy==2.3.1
plotly==6.2.0
SQLAlchemy==2.0.46
psycopg2-binary==2.9.10
openpyxl==3.1.5
