# Floors only. Streamlit Community Cloud runs Python 3.14, which only has
# prebuilt wheels for the newest pandas/numpy/psycopg2 — pinning older exact
# versions forces a source build that fails. Let pip take the newest.
streamlit>=1.46
streamlit-authenticator==0.4.2
pandas>=2.2
plotly>=5.24
SQLAlchemy>=2.0
psycopg2-binary>=2.9.9
openpyxl>=3.1
